# Roadmap

Φάσεις υλοποίησης. Κάθε φάση κλείνει με commit και με κάτι που **φαίνεται** —
ποτέ δύο φάσεις χωρίς ορατό αποτέλεσμα.

---

## Φάση 0 — Απογραφή & υλικό ✅
Η μόνη φάση όπου η δουλειά ήταν κυρίως δική σου.

- [x] Καταγραφή αποφάσεων ([DECISIONS.md](DECISIONS.md))
- [x] Χάρτης σελίδας & content model ([CONTENT.md](CONTENT.md))
- [x] Επιλογή project: **9 σε 3 κατηγορίες**, κλειδωμένη ([CONTENT.md §3](CONTENT.md))
- [x] Απογραφή κώδικα & git ιστορικού ανά project ([INVENTORY.md](INVENTORY.md))
- [x] Draft case study ανά project — γραμμένο από την ανάγνωση του κώδικα
- [x] Σάρωση D9: έλεγχος για εκτεθειμένα διαπιστευτήρια σε όλα τα repos
- [x] **Finnhub API key**: ανακλήθηκε και αφαιρέθηκε από τα 5 αρχεία → `.env`
- [x] Oil Site: **πελάτης** → παρουσιάζεται ανώνυμα, χωρίς όνομα brand
- [x] Ακρίβεια μοντέλου **μετρήθηκε: 90,1%** σε 6.493 συναλλαγές, 32 κατηγορίες
- [x] Stock Valuation: **24.426 εταιρείες** — μετρήθηκε από τη βάση
- [x] Nutrition Tracker: **70 τροφές**
- [x] n8n: δεν υπάρχει JSON (δωρεάν λογαριασμός) → διάγραμμα ροής
- [x] Ενώσεις: τα δύο ETL σε ένα · Διαύγεια + ΚΗΜΔΗΣ σε ένα
- [x] Duty Scheduler: **εγκρίθηκε** — δεν υπάρχει κανόνας που να το απαγορεύει
- [x] `git init` + repo — 5 project έχουν repo· τα υπόλοιπα 4 είναι `caseStudyOnly`
- [x] Βιογραφικό εντοπίστηκε: `Desktop/Υπολογιστής/CV_REVISED.docx` (Αύγ 2025)
- [x] LinkedIn & GitHub URLs, σπουδές, δεξιότητες, πιστοποιήσεις, δημοσιεύσεις
- [x] **Εμπειρία εργασίας** — αντλήθηκε από το LinkedIn: 4 θέσεις με ημερομηνίες
- [x] **Φωτογραφία προφίλ** — από το LinkedIn (200×200, η μόνη διαθέσιμη)
- [x] **Screenshots ανά project** — ολοκληρώθηκε στις Φάσεις 6γ–6ε, και για τα 9

**Τίποτα δεν απομένει εδώ.** Τα screenshots έγιναν στις Φάσεις 6γ–6ε. Τα project
χωρίς repo δεν χρειάστηκαν `git init`: παρουσιάζονται ως `caseStudyOnly` — η
διέξοδος που προβλέπει ρητά η D15, όχι παράλειψη.

## Φάση 1 — Υποδομή & πρώτο deploy ✅
- [x] `git init`, **public repo**: [oikonomouvasilis/portfolio](https://github.com/oikonomouvasilis/portfolio)
- [x] Next.js 16 + TS + Tailwind 4 scaffold (χειροκίνητο — βλ. σημείωση)
- [x] Routing δύο γλωσσών: `/[locale]` με `en` | `el`, redirect από `/`
- [x] `LocaleSwitch` που κρατά τον χρήστη στην ίδια σελίδα
- [x] `hreflang` alternates στα metadata
- [x] ESLint (flat config) + `typecheck` script — και τα δύο καθαρά
- [x] Επαλήθευση: `/` → 307 → `/en` · `/en` 200 · `/el` 200 · `/xx` 404
- [x] Επαλήθευση: `lang` σωστό ανά γλώσσα, μηδέν console errors, καθαρό σε 375px
- [x] **Vercel**: συνδέθηκε και ξεκλείδωσε — https://portfolio-oiko4.vercel.app

**Παραδοτέο:** ζωντανό URL από την πρώτη μέρα. Κάθε επόμενο push ανεβαίνει μόνο του.

**Σημειώσεις υλοποίησης**
- Το `create-next-app` αρνείται όνομα φακέλου με κεφαλαία (`PORTFOLIO`). Το scaffold
  έγινε χειροκίνητα — καλύτερα ούτως ή άλλως, δεν κουβαλήσαμε boilerplate προς διαγραφή.
- Το root `app/layout.tsx` είναι pass-through ώστε το `<html lang>` να ζει στο
  `app/[locale]/layout.tsx` και να αντιστοιχεί στη γλώσσα της σελίδας.
- Το `eslint-config-next` 16 εξάγει flat configs· το `FlatCompat` σκάει σε κυκλική
  αναφορά και αφαιρέθηκε μαζί με το `@eslint/eslintrc`.
- Το `next-mdx-remote` 5.0.0 είχε high-severity advisory (GHSA-g4xw-jxrg-5f6m) —
  αναβαθμίστηκε σε 6.x. `npm audit`: 0 ευπάθειες.

## Φάση 2 — Content pipeline ✅
- [x] zod schema για το frontmatter των project (`lib/content/schema.ts`)
- [x] Loader που διαβάζει τα MDX, επικυρώνει, και **σπάει το build σε λάθος**
- [x] Έλεγχος συμφωνίας slug αρχείου ↔ slug frontmatter
- [x] Fallback γλώσσας με ορατή ειδοποίηση, αντί για σιωπηλή αγγλική σελίδα (D4)
- [x] Παραγωγή κατηγοριών/τεχνολογιών **από τα δεδομένα** (`lib/content/taxonomy.ts`)
- [x] 2 πραγματικά project ως δείγμα, σε EN + EL — `ai-trader`, `nutrition-tracker`
- [x] `content/cv.ts` με typed δομή βιογραφικού (σκελετός, περιμένει υλικό)
- [x] Σελίδες `/projects` και `/projects/[slug]` με MDX rendering

**Παραδοτέο:** προσθέτεις αρχείο → εμφανίζεται στο site χωρίς να αγγίξεις κώδικα.

**Επαληθεύτηκε**
- Build: 11 στατικές σελίδες, όλα τα project × 2 γλώσσες. `typecheck`/`lint` καθαρά.
- Άκυρο frontmatter → build **απέτυχε** με exit 1 και μήνυμα που ονομάζει αρχείο
  και πεδία (`categories.0: Invalid option…`, `stack: Too small…`).
- Αφαίρεση του `nutrition-tracker.el.mdx` → η ελληνική σελίδα σέρβιρε το αγγλικό
  κείμενο **με** την ειδοποίηση fallback· το `ai-trader`, που έχει ελληνικά, καμία.
- Οι όψεις μετρούν σωστά (`TypeScript 2`), και η κατηγορία `data` **δεν**
  εμφανίζεται όσο κανένα project δεν την έχει — καμία άδεια επιλογή φίλτρου.

**Εκκρεμότητες περιεχομένου:** και τα δύο δείγματα έχουν `{/* TODO Φ0 */}` στα
σημεία όπου λείπουν νούμερα. Κανένα νούμερο δεν εφευρέθηκε.

## Φάση 3 — Design system ✅
- [x] Editorial κατεύθυνση (D14): serif επικεφαλίδες, grid, γενναιόδωρο κενό
- [x] Τυπογραφία **με ελληνική κάλυψη**: Source Serif 4 + Inter, self-hosted
- [x] Χρωματικά tokens σε τρεις καταστάσεις: system / light / dark
- [x] Διακόπτης θέματος τριών θέσεων, με μνήμη και χωρίς λευκή αναλαμπή (D16)
- [x] Header με menu, footer, skip-to-content link
- [x] Σκελετικές σελίδες `/about`, `/cv`, `/contact` — κανένας σπασμένος σύνδεσμος
- [x] Επέκταση schema για τις απαιτήσεις D15
- [x] `StackDiagram` — το stack ως στρώματα αρχιτεκτονικής, όχι σειρά από badges
- [x] `Gallery` — φωτογραφικό υλικό με λεζάντες που χρησιμεύουν και ως `alt`
- [x] Ανασχεδιασμός αρχικής, `/projects` και σελίδας project
- [x] `metadataBase` ώστε οι OG εικόνες να δουλεύουν σε social shares

**Επαληθεύτηκε**
- 17 στατικές σελίδες. `typecheck` / `lint` / `npm audit` καθαρά.
- Γραμματοσειρές **όντως φορτώνονται** (`Source Serif 4`, `Inter` — όχι fallback).
- Τα ελληνικά κεφαλαία χάνουν σωστά τον τόνο (`ΑΥΤΟΜΑΤΙΣΜΟΙ`) — δουλεύει μόνο
  επειδή το `lang="el"` είναι σωστό.
- Διακόπτης θέματος: πλήρης κύκλος `system → light → dark → system`, με το
  background να αλλάζει και το εικονίδιο να ακολουθεί (`◐ ☀ ☾`).
- Σελίδα project: 4 πεδία αφήγησης, 4 στρώματα stack, 2 εικόνες, 2 σύνδεσμοι.
- Καμία οριζόντια υπερχείλιση στα 375px. Μηδέν console errors σε καθαρή φόρτωση.

**Δύο σφάλματα που βρέθηκαν και διορθώθηκαν**
1. Ο διακόπτης θέματος **κολλούσε** μετά το πρώτο κλικ: ο handler διάβαζε την τιμή
   από το closure του render, οπότε δύο κλικ στο ίδιο tick έβλεπαν την ίδια παλιά
   τιμή. Λύθηκε διαβάζοντας την τρέχουσα τιμή τη στιγμή του κλικ.
2. `z.record` με enum κλειδιά απαιτεί **όλα** τα κλειδιά — έσπαγε κάθε project που
   δεν είχε και τα τέσσερα στρώματα. Αντικαταστάθηκε με ρητά optional πεδία.

**Εκκρεμότητα:** οι εικόνες είναι placeholders. Το editorial ύφος στηρίζεται στις
εικόνες — μόλις δώσεις screenshots, το site αλλάζει επίπεδο.

## Φάση 4 — Αρχική, About, Contact ✅
- [x] Hero με περίληψη από το `cv.ts`, φωτογραφία προφίλ
- [x] Featured projects (από το flag `featured`)
- [x] Δεξιότητες ομαδοποιημένες κατά τομέα, σε αρχική και `/about`
- [x] `/about`: εμπειρία (4 θέσεις), σπουδές (3), δεξιότητες, ενδιαφέροντα
- [x] `/contact`: email, LinkedIn, GitHub + διακριτική ένδειξη «open to work» (D5)
- [x] Header με menu + διακόπτες, footer — από τη Φάση 3
- [x] `lib/format.ts`: μορφοποίηση περιόδων με σωστούς ελληνικούς μήνες

**Πηγές δεδομένων**
- Σπουδές, δεξιότητες, ενδιαφέροντα: `CV_REVISED.docx`
- Εμπειρία με ημερομηνίες: προφίλ LinkedIn, μέσω της επέκτασης Chrome
- Φωτογραφία: LinkedIn, **200×200** — η μόνη διαθέσιμη ανάλυση

**Επαληθεύτηκε**
- 17 στατικές σελίδες, `typecheck` / `lint` καθαρά.
- `/el/about`, `/el/contact`, `/en/about` → 200. Η φωτογραφία σερβίρεται.
- Μηδέν στοιχεία εκτός viewport στα 1280px.
- Ελληνικοί μήνες σωστά: «Ιούν 2024 – σήμερα», «Οκτ 2019 – Οκτ 2023».

**Αποφάσεις D9 σε αυτή τη φάση**
- Η **ημερομηνία γέννησης** και η **ακριβής οδός κατοικίας** που υπάρχουν στο
  βιογραφικό **δεν** μπήκαν: το repo είναι δημόσιο και δεν τα χρειάζεται κανείς.
- Ο εργασιακός φορέας κρατήθηκε — δηλώνεται ήδη δημόσια στο LinkedIn.

**Εκκρεμότητα:** η φωτογραφία είναι 200×200. Αν βρεθεί η πρωτότυπη (από κινητό ή
φωτογράφο), αντικαθίσταται και εμφανίζεται μεγαλύτερη· τώρα περιορίζεται σε 128px.

## Φάση 5 — Projects (η καρδιά) ✅
- [x] `/projects`: φίλτρα κατηγορίας **και** τεχνολογίας, συνδυαστικά
      (**Ή** μέσα σε κάθε άξονα, **ΚΑΙ** μεταξύ αξόνων)
- [x] Τα φίλτρα γράφονται στο URL (`?cat=automation&tech=Python`) → μοιράζεται
- [x] Αναζήτηση κειμένου με **ελληνική κανονικοποίηση** — τόνοι και τελικό σίγμα
- [x] Μετρητής αποτελεσμάτων με `aria-live` και κουμπί καθαρισμού
- [x] `/projects/[slug]`: case study — έγινε στη Φάση 3
- [x] Πλοήγηση «επόμενο / προηγούμενο project», χωρίς κύκλωση

**Επαληθεύτηκε**
- Συνδυαστικό: `(automation ή fullstack) και Python` → μόνο το AI Trader.
- Deep link `?cat=automation&tech=Python` φορτώνει με τα κουμπιά ήδη ενεργά.
- Αναζήτηση: `αυτοματισμοι`, `ΑΥΤΟΜΑΤΙΣΜΟΊ` και `διατροφη` βρίσκουν σωστά —
  ανεξάρτητα από τόνους και πεζά/κεφαλαία.
- Πολλαπλοί όροι στενεύουν το αποτέλεσμα (`python 2026`), δεν το πλαταίνουν.
- Στο πρώτο project εμφανίζεται μόνο «Επόμενο». Μηδέν console errors.

**Τρία σφάλματα που βρέθηκαν στη δοκιμή**
1. Δύο γρήγορα κλικ σε φίλτρα **έχαναν το ένα**: ο handler διάβαζε τα φίλτρα από
   το closure του render. Ίδια ρίζα με το σφάλμα του διακόπτη θέματος στη Φάση 3.
2. Η πρώτη διόρθωση (ανάγνωση από `window.location`) **δεν αρκούσε**: το
   `router.replace` είναι ασύγχρονο, οπότε το δεύτερο κλικ έβλεπε ακόμη το παλιό
   URL. Λύθηκε κρατώντας την πρόθεση σε ref μέχρι να φτάσει το URL.
3. Η αναζήτηση `αυτοματισμος` **δεν έβρισκε τίποτα**: το ευρετήριο περιείχε το
   slug (`automation`) αλλά όχι τη μεταφρασμένη ετικέτα που βλέπει ο χρήστης.

## Φάση 6 — CV & PDF ✅
- [x] Σελίδα `/cv` από το `content/cv.ts`, και στις δύο γλώσσες
- [x] `npm run cv:pdf` → `public/cv-en.pdf` + `public/cv-el.pdf`
- [x] Κουμπί λήψης σε `/cv`, στο footer και στην αρχική
- [x] Print stylesheet — **είναι ταυτόχρονα η πηγή των PDF** (βλ. αναθεωρημένο D7)

**Πώς παράγονται τα PDF**
Το `scripts/build-cv-pdf.mjs` σηκώνει τοπικό server και τυπώνει τη σελίδα `/cv`
με το **ήδη εγκατεστημένο Chrome** σε headless. Καμία νέα εξάρτηση, και —το
σημαντικό— καμία δυνατότητα απόκλισης μεταξύ σελίδας και PDF: είναι το ίδιο πράγμα.

**Επαληθεύτηκε**
- `cv-en.pdf` 112 KB · `cv-el.pdf` 148 KB, **2 σελίδες** το καθένα.
- Τα **ελληνικά βγαίνουν σωστά** — εξήχθη κείμενο από το PDF και ελέγχθηκε:
  «Αξιωματικός οικονομικού με υπόβαθρο…», «ΕΜΠΕΙΡΙΑ» χωρίς τόνο στα κεφαλαία.
- Η serif επικεφαλίδα φορτώνεται και στο PDF.
- `/cv-el.pdf` και `/cv-en.pdf` σερβίρονται ως `application/pdf`.

**Δύο σφάλματα που βρέθηκαν στη δοκιμή**
1. Το πρώτο PDF βγήκε **χωρίς όνομα**: ο κανόνας `header { display: none }` του
   print stylesheet έκρυβε και τον `<header>` της ίδιας της σελίδας `/cv`, όχι
   μόνο του site. Περιορίστηκε σε `body > header`.
2. `spawn EINVAL` στα Windows όταν το script καλούσε `npx.cmd`. Το binary του
   Next καλείται πλέον απευθείας με `node`, χωρίς shell.

## Φάση 6β — Τα υπόλοιπα case studies ✅
Η βιτρίνα είχε 2 από τα 8 project που αποφασίστηκαν στη Φάση 0. Τώρα έχει όλα.

- [x] `expenses-predictor` — fullstack + data, case study χωρίς repo (private)
- [x] `duty-scheduler` — automation + fullstack, case study χωρίς repo (private, ανώνυμα)
- [x] `etl-stock-valuation` — data, με το πραγματικό διάγραμμα αρχιτεκτονικής
- [x] `n8n-job-seeker` — automation, με το πραγματικό διάγραμμα ροής
- [x] `greek-public-registries` — automation + data, Διαύγεια + ΚΗΜΔΗΣ ενωμένα
- [x] `oil-site` — fullstack, **ανώνυμα**: ούτε όνομα brand ούτε σύνδεσμος repo
- [x] Το `nutrition-tracker` πήρε το μετρημένο «70 τροφές» που έλειπε
- [x] **Διαρροή D9**: το όνομα του πελάτη ήταν γραμμένο σε 3 σημεία των `docs/`
      — αφαιρέθηκε από το HEAD (παραμένει στο git history)

**Επιλογές που θέλουν την έγκρισή σου**
- Δεύτερη κατηγορία `data` στο Expenses Predictor και στα Ελληνικά Μητρώα
  (πρόταση του INVENTORY) → η ισορροπία είναι πλέον 4 / 3 / 4.
- **Στην αρχική προβάλλονται 4**: AI Trader, Expenses Predictor, Duty Scheduler,
  ETL Stock Valuation. Το Nutrition Tracker βγήκε από τα featured (μένει στο `/projects`).
- Το `oil-site` δεν συνδέεται με το δημόσιο repo του: το wordmark υπάρχει σε κάθε
  οθόνη, οπότε ο σύνδεσμος θα ακύρωνε την ανωνυμία.

**Επαληθεύτηκε**
- 29 στατικές σελίδες (από 17), 8 project × 2 γλώσσες. `typecheck` / `lint` καθαρά.
- Καμία γλώσσα σε fallback: όλα τα project έχουν και τις δύο εκδόσεις.
- Φίλτρο `?cat=data` → 3 αποτελέσματα. Αναζήτηση `διαυγεια` (χωρίς τόνο) → 1.
- Καμία οριζόντια υπερχείλιση στα 375px. Μηδέν console errors.

**Δύο που βρέθηκαν στη δοκιμή**
1. Το φίλτρο τεχνολογιών είχε **και `Finnhub` και `Finnhub API`** ως ξεχωριστές
   επιλογές — ενοποιήθηκαν.
2. Η αναζήτηση `Διαύγεια` **δεν έβρισκε τίποτα**: το ευρετήριο διαβάζει
   τίτλο και περίληψη, όχι το σώμα του case study. Η περίληψη ονομάζει
   πλέον ρητά τη Διαύγεια και το ΚΗΜΔΗΣ.

**Εκκρεμότητες περιεχομένου** — γραμμένες ως `{/* TODO Φ0 */}` μέσα στα αρχεία:
χρόνοι πριν/μετά, πλήθος ατόμων στο Duty Scheduler, αν είναι online το oil-site,
αν ανοίγει το repo του Expenses Predictor. **Κανένα νούμερο δεν εφευρέθηκε.**

## Φάση 6γ — Πραγματικά screenshots ✅
Οι εικόνες ήταν placeholders από τη Φάση 3. Τώρα είναι στιγμιότυπα από τα ίδια τα
συστήματα, τραβηγμένα με headless Chrome (CDP), Excel COM και καταγραφή παραθύρου.

- [x] **AI Trader** — 4 από το ζωντανό dashboard: overview, trades, research, positions
- [x] **Duty Scheduler** — 4 από τοπικό τρέξιμο με δεδομένα επίδειξης
- [x] **Oil Site** — 3, χωρίς κανένα wordmark στο κάδρο (D9)
- [x] **ETL Stock Valuation** — η αναφορά Power BI, ανοιγμένη στο Desktop
- [x] **Ελληνικά Μητρώα** — πριν/μετά, με πραγματικά δημόσια δεδομένα
- [x] **Νέο project `personal-expenses`** — το Excel από όπου ξεκίνησαν όλα
- [x] **Nutrition Tracker & Expenses Predictor** — χρειάζονταν σύνδεση του χρήστη
      (Supabase auth). Λύθηκε στις Φάσεις 6γ και 6ε· κανένα placeholder δεν έμεινε.

**Πώς τραβήχτηκαν**
- Οδηγός πάνω από το Chrome DevTools Protocol με το ήδη εγκατεστημένο Chrome —
  καμία νέα εξάρτηση, ίδια λογική με το `cv:pdf`.
- Το Excel εξάγει περιοχές ως PNG μέσω COM (`CopyPicture` → `Chart.Export`), πάνω
  σε **αντίγραφο** — το πρωτότυπο δεν άνοιξε ποτέ για εγγραφή.
- Το Duty Scheduler έτρεξε σε **ξεχωριστή βάση** (`DATABASE_PATH`), με 14
  φανταστικά ονόματα και ουδέτερες ετικέτες («Επίπεδο Α/Β/Γ», «Βάρδια εισόδου») —
  καμία αναφορά σε βαθμολόγιο ή μονάδα (D9). Η πραγματική βάση δεν αγγίχτηκε.
- Για τα Μητρώα φτιάχτηκε ένα `ada.xlsx` με **δημόσιους** αριθμούς πρωτοκόλλου από
  το opendata API, και έτρεξε από πάνω το πραγματικό script. Το αρχείο του χρήστη
  δεν χρησιμοποιήθηκε.

**Διορθώθηκε εν τη ρύμη**
- Το AI Trader έγραφε **61 κλεισμένες συναλλαγές** — το dashboard λέει **67**.
- Το case study του oil-site ισχυριζόταν Κορινθία και «νέο brand χωρίς επινοημένη
  ιστορία». Το χτισμένο site λέει **Μεσσηνία · Est. 1984**. Οι ισχυρισμοί
  αφαιρέθηκαν — **εκκρεμεί απάντηση ποιο ισχύει.**
- Το `products.html` του oil-site **δεν μπήκε**: τα μπουκάλια στις φωτογραφίες
  κουβαλούν το wordmark, ενώ το μενού λέει «BRAND LOGO».

**D9 που σημειώθηκε και δεν λύθηκε**
Το δημόσιο repo `Personal-Expenses` περιέχει το ίδιο το `.xlsx` με **778
πραγματικές οικονομικές εγγραφές** (Οκτ 2023 – Αυγ 2025): μισθός, ενοίκιο,
φαρμακείο, ποσά και υπόλοιπα. Ο χρήστης ενημερώθηκε και **αποφάσισε να μείνει ως
έχει** (21 Σεπ 2026), οπότε τα screenshots δείχνουν πραγματικά νούμερα.

## Φάση 6δ — Όλες οι οθόνες, και carousel ✅
Κάθε project δείχνει πλέον ολόκληρες οθόνες, όχι κομμένα τμήματα, και οι εικόνες
εναλλάσσονται με κλικ.

- [x] **Expenses Predictor** — και τα 5 παράθυρα (Home, Data, Balance, Stats, Forecast)
- [x] **Nutrition Tracker** — και τα 6 (Αρχική, Τροφές, Συνταγές, Ημερολόγιο, Στατιστικά, Πλάνο)
- [x] **Oil Site** — και οι 5 σελίδες, με τις φιάλες θολωμένες (D9 — κουβαλούν το wordmark)
- [x] **n8n** — ο πραγματικός καμβάς ως εξώφυλλο, με το στάδιο αξιολόγησης σε κοντινό
- [x] **ETL** — ολόκληρος ο καμβάς της αναφοράς Power BI αντί για κομμένο γράφημα
- [x] `components/carousel.tsx` — στην κάρτα κάθε project **και** στην πρώτη
      εικόνα της σελίδας του
- **35 εικόνες συνολικά**, 11 MB

**Αποφάσεις του carousel**
- Το κλικ πάνω στην εικόνα πάει στην επόμενη· βέλη, κουκκίδες και μετρητής `ν/Ν`.
- Καμία αυτόματη περιστροφή: μια εικόνα που κινείται μόνη της δίπλα σε κείμενο
  εμποδίζει το διάβασμα, και το στοιχείο δεν είναι διαφήμιση.
- Σταθερό κουτί **16:9 με `object-contain`**: οι οθόνες δεν έχουν όλες την ίδια
  αναλογία (το φύλλο Excel είναι σχεδόν τετράγωνο). Με `cover` θα κόβαμε το μισό,
  με ελεύθερο ύψος θα πηδούσε η σελίδα σε κάθε κλικ.
- Στην κάρτα, η εικόνα **βγήκε από τον σύνδεσμο**: το κλικ αλλάζει εικόνα, ο
  τίτλος πάει στο project. Αλλιώς το ένα κλικ θα έκανε δύο πράγματα.
- Τα βέλη πιάνουν `←` `→` μόνο όσο η εστίαση είναι μέσα στο carousel, και σε
  οθόνη αφής μένουν μόνιμα ορατά αφού δεν υπάρχει hover.

**D9 στα νέα στιγμιότυπα**
- Expenses Predictor: καλύφθηκαν περιγραφές, ποσά, η κατηγορία των τυχερών
  παιγνίων και η φωτογραφία προφίλ — με CSS πριν τη λήψη, όχι επεξεργασία μετά.
  Καλύφθηκε επίσης η μπάρα σελιδοδεικτών που φαίνεται μέσα στο tutorial video.
- Oil Site: οι φιάλες στη σελίδα προϊόντων θολώνουν· οι ετικέτες είναι το σήμα
  του πελάτη.

**Σφάλματα των ίδιων των εφαρμογών** — δεν διορθώθηκαν, είναι άλλα repos:
- Expenses Predictor: ~~τα γραφήματα δεν σχεδιάζονται~~ — **λάθος διάγνωση, βλ.
  Φάση 6ε.** Θέλουν ρητό εύρος ημερομηνιών, το backend στο Render είχε
  αδρανοποιηθεί, και η λήψη έπεφτε μέσα στο animation του Recharts.
- NutriDash: ανεστραμμένος κάθετος άξονας στο «Ημερήσια πρόσληψη».

## Φάση 6ε — Φορτωμένα γραφήματα, Κορινθία, hover ✅

- [x] **Κορινθία** — επιβεβαιώθηκε ως η περιοχή του oil-site και μπήκε στο κείμενο.
      Το χτισμένο build γράφει ακόμη «Μεσσηνία · Est. 1984»· δεν το επαναλαμβάνουμε.
- [x] **Expenses Predictor** — ξαναλήφθηκαν και οι 5 οθόνες με τα γραφήματα
      φορτωμένα. Αντικαταστάθηκαν οι παλιές.
- [x] **Hover στη λίστα** — στο `/projects` η εικόνα αλλάζει με το ποντίκι· μέσα
      στο project παραμένει το κλικ.

**Διόρθωση προηγούμενης διάγνωσης**
Είχα γράψει ότι τα γραφήματα του Expenses Predictor είναι σπασμένα. Δεν ήταν.
Δύο πράγματα έφταιγαν, κανένα από τα δύο σφάλμα σχεδίασης:

1. **Θέλουν ρητό εύρος ημερομηνιών.** Με κενά τα πεδία, τα components δεν
   προσαρτώνται καν — η σελίδα δείχνει *NO DATA AVAILABLE*.
2. **Το backend στο Render αδρανοποιείται.** Στις πρώτες λήψεις απαντούσε μετά
   από 60 δευτερόλεπτα, οπότε η εφαρμογή έδειχνε 0 εγγραφές. Μετά από warm-up
   γύρισαν και οι **5.590**.

Το script λήψης συμπληρώνει πλέον τις ημερομηνίες και **περιμένει να εμφανιστούν
πραγματικά σχήματα** (τομείς πίτας, καμπύλες, μπάρες) πριν τραβήξει — όχι απλώς
να περάσει χρόνος.

**Ούτε η τροχιά της πρόβλεψης ήταν σφάλμα.** Το Recharts εμφανίζει κάθε σειρά
πίσω από ένα `animationClipPath`. Η λήψη έπεφτε μέσα στο animation, οπότε η
καμπύλη φαινόταν σαν λωρίδα δέκα pixel στην αριστερή άκρη. Αφαιρώντας το clip
πριν το screenshot, σχεδιάζεται ολόκληρη: εξάμηνη προβολή με ορατή εβδομαδιαία
εποχικότητα. **Καμία από τις τρεις οθόνες δεν είχε τελικά bug.**

**Το redaction δούλευε σε στοιχεία — τώρα σε κόμβους κειμένου**
Το «21.557,11» και το «€» ζουν σε χωριστά στοιχεία, οπότε ο έλεγχος «μόνο φύλλα»
άφηνε το νούμερο ακάλυπτο. Πιάνονται πλέον και σκέτοι αριθμοί σε ελληνική μορφή.

**Πώς δουλεύει το hover**
Κάθετες λωρίδες, μία ανά εικόνα, ως απλά `div` — όχι κουμπιά. Έτσι ολόκληρη η
κάρτα ξαναέγινε σύνδεσμος: το ποντίκι ξεφυλλίζει, το κλικ πάει στο project.
Φεύγοντας, επιστρέφει στην πρώτη εικόνα. Με πληκτρολόγιο φαίνεται μόνο η πρώτη —
αποδεκτό, αφού η πλήρης σειρά υπάρχει με κλικ μέσα στο project.

## Φάση 7 — Polish & go-live ✅ *(εκτός από όσα θέλουν εσένα)*
- [x] SEO: metadata ανά σελίδα, sitemap.xml, robots.txt, `hreflang` για τις 2 γλώσσες
- [x] OG images — παραγόμενες δυναμικά ανά project
- [x] JSON-LD `Person` schema (βοηθά να σε βρίσκουν στο Google)
- [x] Lighthouse ≥ 95 και στους 4 άξονες — **100 / 100 / 100 / 100** στο live· βλ. παρακάτω
- [x] Προσβασιμότητα: πλοήγηση μόνο με πληκτρολόγιο, contrast, alt κείμενα
- [ ] **Έλεγχος σε κινητό, σε πραγματική συσκευή** — δική σου δουλειά
- [x] 404 σελίδα
- [x] Analytics (D10)
- [x] **Τελικός έλεγχος D9** — secret scanning, καμία διαρροή δεδομένων
- [ ] Custom domain αν έχει αποφασιστεί (D11) — **η D11 είναι ακόμη ανοιχτή**.
      Προσωρινά το `portfolio-oiko4.vercel.app` είναι πλέον κανονικό production
      domain, οπότε τίποτα δεν επείγει· βλ. «Το site ήταν `noindex`» παρακάτω.
- [ ] **GitHub προφίλ** — bio, τοποθεσία και **link στο site** στο
      [github.com/settings/profile](https://github.com/settings/profile).
      Είναι κενό· όποιος φτάνει εκεί από το portfolio βλέπει άδεια σελίδα.
      *(ζητήθηκε ρητά ως υπενθύμιση — 15 Σεπ 2026)*

**Το site ήταν `noindex` και κανείς δεν το ήξερε**
Τοπικά το Lighthouse έβγαζε 99 / 100 / 96 / 69. Απέδωσα και τους δύο χαμηλούς
βαθμούς σε τεχνουργήματα του τοπικού περιβάλλοντος. Για τον έναν είχα δίκιο, για
τον άλλον **λάθος** — και το λάθος ήταν το σοβαρό.

- *Best practices 96.* Σωστή διάγνωση: ένα 404 στο `/_vercel/insights/script.js`,
  που υπάρχει μόνο όταν το σερβίρει το Vercel. Στο live έγινε 100.
- *SEO 69 — «Page is blocked from indexing».* **Λάθος διάγνωση.** Νόμιζα ότι
  φταίει το `robots.txt`, που γράφει `Disallow: /` εκτός production. Στο live
  όμως το `robots.txt` έγραφε κανονικά `Allow: /` — και το SEO έμεινε 69.

Η αιτία ήταν στα headers, όχι στο `robots.txt`:

```
curl -sI https://portfolio-oiko4.vercel.app/en
X-Robots-Tag: noindex
```

Το `portfolio-oiko4.vercel.app` **δεν ήταν καταχωρημένο domain του project** — ήταν
alias. Το Vercel βάζει `noindex` σε κάθε `*.vercel.app` που δεν είναι το δηλωμένο
production domain. Το project είχε ένα μόνο domain, το αυτόματο
`portfolio-rust-omega-u2zt9n4ih5.vercel.app`, και μόνο εκείνο ήταν indexable.

Το πρόβλημα **προϋπήρχε της Φάσης 7** — το URL ήταν ήδη hardcoded στο
`metadataBase`. Αλλά η Φάση 7 το έκανε να κουβαλάει τα πάντα: κάθε canonical, όλο
το sitemap και κάθε OG διεύθυνση έδειχναν σε σελίδα που λέει στην Google να μην
την ευρετηριάσει. Είναι επίσης η διεύθυνση που είναι **τυπωμένη στα PDF του
βιογραφικού** — για άνθρωπο δούλευε, για μηχανή αναζήτησης ήταν αόρατη.

**Η διόρθωση**, 22 Σεπ 2026: το `portfolio-oiko4.vercel.app` προστέθηκε στα
Domains του project με προορισμό Production. Καμία αλλαγή κώδικα — το `siteUrl`
ήδη εκεί έδειχνε. Το `X-Robots-Tag` έφυγε αμέσως.

**Μετρημένο στο live μετά τη διόρθωση: 100 / 100 / 100 / 100**, σε αρχική και σε
case study.

*Το μάθημα: ένα `robots.txt` που λέει `Allow` δεν σημαίνει ότι η σελίδα είναι
indexable. Το `X-Robots-Tag` υπερισχύει και δεν φαίνεται πουθενά στη σελίδα.*

**Το «αχνό» γκρι δεν περνούσε το AA**
Το `--faint` είναι το χρώμα των μικρών κεφαλαίων ετικετών — 11px κείμενο. Έδινε
**3.36:1** στο ανοιχτό θέμα και **4.05:1** στο σκούρο· το όριο του WCAG AA για
κανονικό κείμενο είναι 4.5:1. Ήταν η μόνη πραγματική παράβαση προσβασιμότητας
που βρέθηκε, και δεν φαινόταν με το μάτι.

Τώρα είναι `#706d65` και `#8a867c`, μετρημένα πάνω στο `--surface` και όχι στο
`--bg` — γιατί εκεί κάθονται οι ετικέτες του γραφήματος stack, και το `surface`
είναι η αυστηρότερη από τις δύο βάσεις. Το axe-core βγάζει **μηδέν παραβάσεις**
σε αρχική, λίστα, case study, βιογραφικό και επικοινωνία, σε **αμφότερα** τα
θέματα και σε πλάτος κινητού.

**Το LCP περίμενε το JavaScript**
Η μεγαλύτερη εικόνα της αρχικής είναι μια από τις δύο πρώτες κάρτες «Επιλεγμένη
δουλειά», και φορτωνόταν τεμπέλικα — δηλαδή ο browser δεν ήξερε ότι την
χρειάζεται μέχρι να τρέξει το JS. Με `priority` στις δύο πρώτες, το LCP έπεσε
στα **0,9 δευτερόλεπτα**.

**Οι OG κάρτες χρειάστηκαν δική τους γραμματοσειρά**
Το `ImageResponse` δεν διαβάζει το `next/font`, και το satori από πίσω δεν
διαβάζει WOFF2 — που είναι ακριβώς ό,τι κατεβάζει το `next/font`. Χωρίς ρητή
γραμματοσειρά, το «Πρόγραμμα Υπηρεσιών» θα έβγαινε κουτάκια.

Έτσι το **greek subset** των Source Serif 4 και Inter ζει σε WOFF μέσα στο repo,
στο `assets/fonts/`. Δεν κατεβαίνει στο build: ένα build δεν έχει λόγο να
εξαρτάται από το αν σηκώνει το fonts.gstatic.com.

Οι κάρτες είναι **τυπογραφικές, όχι στιγμιότυπα**. Τα screenshots έχουν διάφορες
αναλογίες και πυκνό περιεχόμενο· περικομμένα σε 1200×630 δεν διαβάζεται τίποτα.
Ο τίτλος διαβάζεται.

**Δύο σελίδες 404, γιατί υπάρχουν δύο περιπτώσεις**
Το `app/[locale]/not-found.tsx` πιάνει ό,τι γεννιέται *μέσα* σε έγκυρη γλώσσα —
ανύπαρκτο slug project — και κρατά κεφαλίδα και υποσέλιδο. Το `app/not-found.tsx`
πιάνει τις εντελώς άγνωστες διευθύνσεις και γράφει **δικό του `<html>`**: το
`app/layout.tsx` είναι pass-through, οπότε αν δεν τα έγραφε, η σελίδα θα έβγαινε
χωρίς `<html>` και `<body>`.

**Το `openGraph` της σελίδας δεν συγχωνεύεται με του layout**
Το Next το **αντικαθιστά**. Κάθε σελίδα που δήλωνε μόνο τίτλο και περιγραφή έχανε
σιωπηλά τα `og:site_name` και `og:locale` — φάνηκε μόνο διαβάζοντας το `<head>`
του χτισμένου HTML. Γι' αυτό υπάρχει το `openGraphFor()` στο `lib/site.ts`:
ό,τι είναι κοινό γράφεται μία φορά και δεν μπορεί να ξεχαστεί.

## Φάση 8 — Ζωντάνια & πλούτος *(σχεδιασμένη 22 Σεπ 2026, ξεκινά 23 Σεπ)*

Το site είναι σωστό. Η Φάση 8 είναι για να γίνει **ελκυστικό**. Δεν διορθώνει
σφάλματα — προσθέτει κίνηση, εικονίδια, καλύτερο φιλτράρισμα και περιεχόμενο.

### 8.1 — Δομή σελίδας & κίνηση ✅
- [x] Έρευνα σε σύγχρονες πρακτικές web design· το site να «ζωντανέψει»
- [x] Animations και transitions που τραβούν το ενδιαφέρον του επισκέπτη
- [x] **Αποφασίστηκε:** κατακόρυφη αφήγηση στην αρχική, ξεχωριστές σελίδες για τα
      case studies (D17) — ο συμβιβασμός που κρατά το SEO
- [x] Νέα παλέτα, νέες γραμματοσειρές, κλίμακες μεγέθους και καμπυλότητας (D17)
- [x] Οι σειρές των επιλεγμένων έργων εναλλάξ: **μικρή εικόνα** στη μία πλευρά,
      **κείμενο δίπλα της** στην άλλη
- [x] Ζώνες πλήρους πλάτους· ο περιορισμός πλάτους έφυγε από το `main` και
      μετακόμισε στο `Container`. **Η αλλαγή φόντου ανά ζώνη αναιρέθηκε στην 8.6**
      (D18): ξεχωρίζουν πλέον με γραμμές, πάνω σε ένα κοινό φόντο

**Μετρημένο μετά την αλλαγή:** απόδοση **100**, προσβασιμότητα **100**, LCP
**0,6s** (από 0,9s), CLS **0**. Μηδέν παραβάσεις axe-core σε αρχική και case
study, σε ανοιχτό και σκούρο θέμα, σε desktop και σε πλάτος κινητού.

**Τι δεν αντιγράψαμε από τα βραβευμένα.** Στο By-Kin — τέσσερα βραβεία, Next.js,
editorial τυπογραφία — το πληκτρολόγιο **δεν σκρολάρει**: επτά Page Down και η
σελίδα ακίνητη. Το scroll-jacking που το προκαλεί είναι και η τεχνική που
υποχωρεί, γιατί ανταγωνίζεται την ικανότητα του browser να απαντήσει. Εδώ η
κίνηση **αντιδρά** στο scroll, δεν το δεσμεύει.

**Η σειρά ανάγνωσης κόστισε μια αναθεώρηση.** Οι μετατοπισμένες σειρές δοκιμάστηκαν
με `flex-row-reverse`, που έβγαζε «2026 → περίληψη → τίτλος». Η ασυμμετρία υπάρχει
ήδη στη θέση και στο πλάτος· δεν χρειάζεται να πληρωθεί με τη σειρά ανάγνωσης.

**Δεύτερη αναθεώρηση: μεγάλες εικόνες, λίγα λόγια.** Η πρώτη εκδοχή έδινε σε κάθε
έργο εικόνα σχεδόν πλήρους πλάτους με το κείμενο από κάτω. Έδειχνε εντυπωσιακή και
άφηνε τον επισκέπτη να βλέπει **ένα** έργο τη φορά — το αντίθετο από ό,τι θέλει μια
βιτρίνα. Οι κατάλογοι του Awwwards είναι πυκνά πλέγματα μικρών εικόνων, ακριβώς
γι' αυτόν τον λόγο.

Τώρα η εικόνα είναι 383px, το κείμενο κάθεται **δίπλα** της, και χωρούν περίπου
**3,3 έργα σε οθόνη 900px** — και τα τέσσερα σε 1.041px συνολικά.

Προστέθηκε και **δεύτερη παράγραφος** από το `outcome`. Η περίληψη είναι 82–136
χαρακτήρες από σχεδιασμό, φτιαγμένη για κάρτα· δίπλα σε εικόνα άφηνε τη στήλη
μισοάδεια και δεν έλεγε τι βγήκε τελικά. Το `outcome` είναι 159–256 χαρακτήρες και
κουβαλάει τα νούμερα: «ακρίβεια 90,1% (±0,8%, 3-fold cross-validation) σε 6.493
συναλλαγές».

**Ένα σφάλμα πλέγματος που φάνηκε μόνο με μέτρηση.** Οι εναλλασσόμενες σειρές
δήλωναν στήλη αλλά **όχι γραμμή**. Η αυτόματη τοποθέτηση του grid έστελνε τότε το
κείμενο —δεύτερο στο DOM, αλλά ζητούσε προηγούμενη στήλη— σε νέα γραμμή, και οι
μισές σειρές έβγαιναν στοιβαγμένες: **456px αντί για 216**. Στο στιγμιότυπο έμοιαζε
με «λίγο παραπάνω κενό». Το `row-start-1` και στα δύο στοιχεία το έλυσε.

> **Εκκρεμεί μέσα στην 8.1:** οι **εσωτερικές** σελίδες κρατούν ακόμη την παλιά
> τυπογραφική γλώσσα — κεφαλαία mono labels με αραιό tracking σε βιογραφικό,
> λίστα έργων, case study, carousel και γράφημα stack. Η αρχική τα άφησε, οι
> υπόλοιπες όχι, και η ασυνέπεια φαίνεται.

> ⚠️ **Πριν ξεκινήσει το τρίτο σημείο, χρειάζεται απόφαση.** Η Φάση 7 έστησε
> εννιά ξεχωριστές διευθύνσεις με canonical, `hreflang`, sitemap και δική της OG
> κάρτα η καθεμία. Αν όλα γίνουν ενότητες μίας σελίδας, οι οκτώ παύουν να
> υπάρχουν: η Google δεν έχει τι να ευρετηριάσει ανά project, και το LinkedIn
> δείχνει την ίδια κάρτα για κάθε σύνδεσμο.
>
> Πιθανός συμβιβασμός: **η αρχική** γίνεται αφηγηματική με κατακόρυφη ροή και
> ενότητες, αλλά τα case studies κρατούν τις δικές τους σελίδες. Κερδίζεις την
> κίνηση χωρίς να χαλάσεις το SEO. Να αποφασιστεί αύριο, πριν γραφτεί κώδικας.

> ⚠️ Δύο πράγματα να μη χαθούν: το `prefers-reduced-motion` που ήδη τιμάται στο
> `globals.css`, και το **Lighthouse 100** στην απόδοση. Κάθε animation περνά και
> από τα δύο.

### 8.2 — Εικόνες Oil Site
- [ ] Εικόνες **ή και βίντεο** που δείχνουν αναλυτικά κάθε σελίδα, όχι μόνο το
      μενού της κεφαλίδας
- [ ] Ισχύει το D9: κανένα wordmark, κανένα στοιχείο του πελάτη στο κάδρο

### 8.3 — Εικονίδια
- [ ] Στους συνδέσμους κώδικα και demo
- [x] Στις **δεξιότητες**, σε αρχική και προφίλ (βλ. 8.6). Μένουν οι λίστες stack
      στις κάρτες και στο γράφημα του κάθε case study
- [x] Σε GitHub και LinkedIn, στην **κεφαλίδα** (8.7). Το υποσέλιδο κρατά τα ίδια
      σε κείμενο
- [x] Σημαίες στον διακόπτη γλώσσας (8.7)

### 8.4 — Φιλτράρισμα σε categories & technologies
- [ ] Δυναμικό φιλτράρισμα με dropdown ή scroll
- [ ] Ουσιαστική δυνατότητα filter — όχι μόνο οπτική αλλαγή

*Σημείωση: το `ProjectExplorer` έχει ήδη αναζήτηση και chip ανά κατηγορία και
τεχνολογία. Η 8.4 είναι εξέλιξη αυτού, όχι νέα κατασκευή από το μηδέν.*

### 8.5 — Προσθήκες στο βιογραφικό
- [ ] Γλώσσες
- [ ] Συμμετοχές σε δράσεις και συνέδρια
- [ ] Πιστοποιήσεις
- [ ] Ό,τι άλλο στοιχείο υπάρχει και λείπει

> 📋 **Θέλει υλικό από εσένα.** Ο κώδικας δεν μπορεί να εφεύρει σε ποια συνέδρια
> ήσουν. Φέρε τα στοιχεία και τα περνάω στο `content/cv.ts` — από εκεί
> ενημερώνονται **μαζί** η σελίδα `/cv` και τα δύο PDF (D7), και θέλουν και τις
> **δύο γλώσσες**, αλλιώς σπάει το build.

### 8.6 — Ενιαίο φόντο, διαδοχική εμφάνιση, εικονίδια δεξιοτήτων ✅ *(23 Σεπ 2026)*
- [x] **Ένα** φόντο για όλο το site, με εικόνες των project να εναλλάσσονται αργά
      από πίσω (`components/backdrop.tsx`). Τέλος οι ζώνες σε διαφορετικά συμπαγή
      χρώματα — ξεχωρίζουν με γραμμές (D18)
- [x] Οι επιφάνειες που έμεναν συμπαγείς (κορνίζες carousel και gallery, κουμπιά
      σε hover) πέρασαν στο ημιδιάφανο `--panel`
- [x] Διαδοχική εμφάνιση: 80ms ανάμεσα σε όσα μπαίνουν μαζί στο κάδρο, και μέσα
      σε κάθε σειρά project εικόνα → τίτλος → περίληψη → αποτέλεσμα
- [x] Εικονίδια σε κάθε δεξιότητα· 15 σήματα από τη Simple Icons (αντιγραμμένα
      τοπικά, το πακέτο μένει devDependency) και 10 γραμμικά σύμβολα για όσα δεν
      έχουν σήμα

**Το τίμημα μετρήθηκε:** με εικόνα από πίσω, η βάση του κειμένου δεν είναι πια
σταθερό χαρτί. Το `--faint` μετακινήθηκε και στα δύο θέματα ώστε η **χειρότερη**
στιγμή —σκούρο στιγμιότυπο πίσω από μικρή γραμματοσειρά— να μένει πάνω από 4,5:1
αντί για 4,3:1. Οι εικόνες περνούν από `contrast(0.75)` πριν μπουν, ακριβώς για να
μη μεταβάλλεται η αντίθεση ανάλογα με το ποιο στιγμιότυπο τυχαίνει να παίζει.

### 8.7 — Κίνηση παντού, παράσταση που δεν σταματά ✅ *(23 Σεπ 2026)*
- [x] Pop in **και** pop out σε όλες τις σελίδες: προφίλ, λίστα project, case
      study, βιογραφικό, επικοινωνία (D19). Δηλώνεται με `data-reveal` ή
      `data-reveal-children` — δύο attributes, καμία αλλαγή στα client/server όρια
- [x] Τρεις κηλίδες φωτός που κινούνται συνέχεια πάνω από το πέπλο, μασκαρισμένες
      έξω από τη στήλη ανάγνωσης
- [x] GitHub και LinkedIn με εικονίδια στην κεφαλίδα· σημαίες στον διακόπτη γλώσσας
- [x] Το πορτρέτο της αρχικής στρογγυλό, 160px, στοιχισμένο στο κέντρο του κειμένου

**Τρία πράγματα που θα έσπαγαν σιωπηλά** και καλύφθηκαν: το PDF του βιογραφικού
(θα τυπωνόταν λευκό), οι κάρτες που ξαναχτίζονται σε κάθε φίλτρο (θα έμεναν
αόρατες), και η ενυδάτωση του `ProjectExplorer` μέσα σε Suspense. Βλ. D19.

> 📋 **Θέλει υλικό από εσένα:** φωτογραφία προφίλ **≥400×400**. Η τωρινή είναι
> 200×200 από το LinkedIn και στα 160px σε οθόνη διπλής πυκνότητας μαλακώνει.

---

## Εκτίμηση

| Φάση | Δουλειά | Ποιος |
|------|---------|-------|
| 0 | Απογραφή & υλικό | **εσύ**, εγώ βοηθώ με τη λίστα |
| 1–3 | Υποδομή, content pipeline, design | εγώ |
| 4–5 | Σελίδες & projects | εγώ, εσύ κρίνεις κείμενα |
| 6–7 | CV, SEO, go-live | εγώ |
| 8 | Ζωντάνια, εικονίδια, φίλτρα | εγώ· **εσύ** για το υλικό του βιογραφικού |

Η Φάση 0 είναι το πραγματικό ρίσκο χρονοδιαγράμματος. Οι υπόλοιπες κυλάνε.

Στη Φάση 8 το ρίσκο είναι αλλού: είναι η πρώτη φάση που **αφαιρεί** κάτι αν
γίνει βιαστικά. Οι προηγούμενες πρόσθεταν.
